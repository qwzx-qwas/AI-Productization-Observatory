# Task Template

处理任务时，固定输出以下结构：

- `canonical_basis`
- `proposed_change`
- `impacted_files`
- `tests_or_acceptance`
- `open_blockers`

若任务涉及未冻结项，必须引用：

- `17_open_decisions_and_freeze_board.md`

并明确说明：

- 是否可按 `current_default` 临时执行
- 是否必须暂停等待冻结
