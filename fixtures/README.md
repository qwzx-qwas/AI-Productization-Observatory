# Fixtures Status

本目录预留给 deterministic fixtures。

当前状态：

- `status = stub`
- 目录已建立
- 样本文件尚未落成

目标子目录：

- `fixtures/collector/`
- `fixtures/normalizer/`
- `fixtures/extractor/`
- `fixtures/scoring/`
- `fixtures/marts/`

对应规范：

- `14_test_plan_and_acceptance.md`
- `16_repo_structure_and_module_mapping.md`

进入“已实现”前的完成条件：

- 至少存在一组可重复运行的 fixture 文件
- 每组 fixture 都能对应到明确模块和验收目标
- 文档状态从 `stub` 更新为 `implemented`
